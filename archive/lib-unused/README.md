# Unused library modules

Code moved here because nothing in the repo `require()`s it anymore. Bench storage for the matchup predictor is inlined in `views/matchup-predictor.ejs` and the static HTML in `docs/`.

| File | Was intended for |
|------|------------------|
| `matchupLineupStorage.js` | `localStorage` helpers for per-matchup bench picks |
