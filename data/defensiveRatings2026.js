/**
 * Optional manual overrides keyed by normalized player name (same as `normalizePlayerName` in server).
 * Populated automatically from `data/templates/mms_defensive_ratings_2026_template.xlsx` when that file exists.
 * Values are numeric defensive scores (interpretation aligns with league sheet convention).
 */
module.exports = {
  normalizedNameToDefense: {},
};
