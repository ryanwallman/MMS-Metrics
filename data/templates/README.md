# Data templates

Place **`mms_defensive_ratings_2026_template.xlsx`** here to auto-populate defensive ratings (see `server.js` / `data/defensiveRatings2026.js`). If the file is missing, bundled defaults in `data/defensiveRatings2026.js` are used.
